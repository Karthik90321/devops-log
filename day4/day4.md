Day 4  - permissions
ls -l: [type][owner rwx][group rwx][other rwx]
chmod +x makes executable. ./ runs file in currrent dir
Security comes from permission bits, not the dot prefix
Shebang #!/bin/bash picks the interpreter
