Day 5 -git internals and branches
3 areas: working dir -> (git add) -> staging -> (git commit) -> repo -> (git push) -> GitHub
checkout switches branches LOCALLY, rewrites working dir. Never touches Gitub.
A branch is just a movable pointer to a commit
origin= nickname for the github remote.
.git folder IS the repository - all history leaves there
